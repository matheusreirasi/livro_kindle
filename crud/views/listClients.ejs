<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>


<body>
    <h1> <%= title %></h1>
    <p>
        Clientes já cadastrados no sistema.
    </p>

    <table>
        <thead>
            <tr>
                <td>Nome</td>
                <td>Idade</td>
                <td>UF</td>
                <td>Ações</td>
            </tr>
        </thead>

        <tbody>
            <% if (!docs || docs.length == 0) { %>
                <tr>
                    <td colspan="4"> Nenhum cliente cadastrado. </td>
                </tr>

            <% } else {docs.forEach(function (customers) { %>
                <tr>
                    <td> <%= customers.nome %></td>
                    <td> <%= customers.idade %></td>
                    <td> <%= customers.uf %></td>
                    <td>
                        <a href="/edit/<%=customers._id %>"> Editar</a>
                        <a href="/delete/<%=customers._id %>" onclick="return confirm('Deseja excluir usuário?')">Excluir</a>
                    </td>
                </tr>
                <% }) <!--Esse é pra função e pro foreach-->
            } %> <!--Esse é pro else, colocando essas tags eu faço tudo isso ficar dentro da função -->
        </tbody>

        <tfoot>
            <tr>
                <td colspan="4">
                    <a href="/">Cadastrar usuário</a>
                </td>
            </tr>
        </tfoot>
        
    </table>

</body>


</html>
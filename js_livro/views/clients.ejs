<!DOCTYPE html>
<html lang="pt-BR>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= title %> </title>
</head>

<body>
    <h1> <%= title %> </h1>
    <p> Clientes já cadastrados no sistema</p>
    <table style="width: 50%">
        <thead>
            <tr style="background-color: #CCC"> <!--tr: table rows-->
                <td style="width: 50%"> ID</td>
                <td style="width: 50%"> Nome </td> <!--td: table data-->
                <td style="width: 15%"> Idade </td>
                <td style="width: 15%"> UF </td>
                <td> Ações </td>
            </tr> 
        </thead>


        <tbody>
            <% if (!docs || docs.length == 0){%> 
                <tr >
                    <td colspan="4"> Nenhum cliente cadastrado </td>
                </tr>  

            <%} else{ docs.forEach(function(customers) {%> <!--O nome da collection tem que ser igual ao do MongoDb e igual em todos os arquivos-->
                <tr>
                    <td style="width:50%">
                        <%= customers.nome %>
                    </td>

                    <td style="width: 15%">
                        <%= customers.idade %>
                    </td>

                    <td style="width:15%">
                        <%= customers.uf %>
                    </td>
                    
                    <td>
                        <a href="/edit/<%= customers._id %>"> Editar</a> <!--Isso me leva para uma rota chamada "/edit" e o ID do cliente no caminho da URL, tem que ser colocado tudo junto-->
                        <a href="/delete/<%= customers._id %>" onclick="return confirm ('Realmente deseja excluir usuário?')"> Excluir</a>
                    </td>
                </tr>
                <% })
            } %>
        </tbody>

        <tfoot>
            <tr>
                <td colspan="4">
                    <a href="/crudClients"> Cadastrar Novo Usuário </a>
                </td>
            </tr>
        </tfoot>
    </table>

</body>
</html>